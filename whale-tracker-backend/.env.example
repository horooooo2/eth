# 默认走官方 Hyperliquid（不配 Key、不设 HL_USE_GOLDRUSH）
# HL_INFO_URL=https://api.hyperliquid.xyz/info
# HL_WS_URL=wss://api.hyperliquid.xyz/ws

# 可选：显式启用 GoldRush（需积分）
# HL_USE_GOLDRUSH=1
# GOLDRUSH_API_KEY=

# 失败回退（默认开；官方为主时几乎用不到）
# HL_INFO_FALLBACK=1

# 并发（官方建议 1～2）
HL_MAX_CONCURRENT=2
# HL_INFO_TIMEOUT_MS=30000
WHALE_SNAPSHOT_CONCURRENCY=2
WHALE_SHARD_SIZE=10

# 仓位分片刷新（毫秒），默认 5 分钟
REFRESH_INTERVAL=300000

# 成交 WS：仅订阅有仓地址（按仓位价值优先），默认最多 60
# FILL_SUB_TOP_N=60
# FILL_SUB_MIN_USD=1000

# 资金动态（fills）保留天数，默认 3；异动仍默认 7 天
# FILL_RETENTION_DAYS=3
# ALERT_RETENTION_DAYS=7

# 重启后慢补成交：默认关；需要时设 FILL_BACKFILL=1
# FILL_BACKFILL=1
# FILL_BACKFILL_INTERVAL_MS=5000
# FILL_BACKFILL_RATE_LIMIT_MS=1800000
# FILL_BACKFILL_DAYS=3

# TOP_WHALE_LIMIT=200
# PORT=3000
