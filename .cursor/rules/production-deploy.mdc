---
description: Production deploy only via existing GitHub Actions; official AlphaExecution is EXECUTE
alwaysApply: true
---

# Production deploy (CI/CD only)

WhaleTracker 生产只走现有 GitHub Actions：

代码 → 本地测试 → commit → push `main` → `.github/workflows/deploy.yml` → pack/upload → `remote-deploy.sh` → systemd → health

禁止：手工 tar/scp/rsync、新写第二套部署脚本、Cursor 本机 SSH 发布、绕过 Actions 改生产文件、手工复制 backend/frontend/engine。

CI 失败只排查：workflow / pack / upload / `remote-deploy.sh` / systemd / health。不要为绕过失败而手工部署。

公网：http://43.160.208.168/#/  health：http://43.160.208.168/api/health

## 配置不得漂移

不要在生产服务器手工改 `.env`。正式值必须在仓库（`remote-deploy.sh` / `.env.example` / workflow）里，由 Actions 带到生产。不要再加 post-deploy SSH flip。

`remote-deploy.sh` 正式写入：

- `V41_ALPHA_EXECUTION=EXECUTE`
- `V41_ENGINE_EXECUTION_MODE=node_gateway`（legacy）
- `V41_LIVE_TRADING_ENABLED=false`
- `V41_QA_LIVE_ENABLED=false`
- `V41_MARKET_DATA_SOURCE=okx`
- `V41_ENGINE_AUTOSTART=0`

EXECUTE 只表示订单可进 Node Gateway。账户仍须由登录用户解析为 `OKX_DEMO`（`simulated=true`），否则 Preflight 禁止启动 S1。禁止 OKX_LIVE。

workflow success ≠ 可启动。验收：`/api/health=200`、`transport_status=CONNECTED`、`alpha_execution=EXECUTE`、`live_permission=false`、Engine=OFFLINE/PAUSED。登录后还要：`user_id_ready`、`account_environment=OKX_DEMO`、recovery READY、BTC FLAT、pending/algo=0、net/cross、leverage ≤ S1 cap、instrument READY。全部 PASS 后才允许手动 select S1 → start。
